# MongoDB Connection String
# Database ini di-share oleh MateGroup SSO dan CoMate
# Collection naming: mategroup_* untuk SSO, comate_* untuk CoMate
MONGO_CONNECTION_STRING=mongodb+srv://<db_username>:<db_password>@mategroup-staging.a9ihnie.mongodb.net/mategroup?retryWrites=true&w=majority&appName=MateGroup-Staging

# Supabase Configuration (HANYA untuk Storage)
# Auth tidak menggunakan Supabase, menggunakan JWT sendiri
SUPABASE_URL=your-supabase-url
SUPABASE_SERVICE_KEY=your-service-key

# JWT Secret untuk autentikasi
JWT_SECRET=your-secret-key-here

# Cloudflare Turnstile (anti-bot verification)
TURNSTILE_SECRET_KEY=your-turnstile-secret-key

# Frontend URL (untuk CORS dan redirect)
FRONTEND_URL=http://localhost:3000

# Server Configuration
PORT=5000
NODE_ENV=development

